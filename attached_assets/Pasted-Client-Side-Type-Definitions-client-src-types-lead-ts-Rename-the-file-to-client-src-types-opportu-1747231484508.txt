Client-Side Type Definitions (client/src/types/lead.ts)

Rename the file to client/src/types/opportunity.ts (or a more general name like crm.ts if it will also hold the new Lead type).
Rename the Lead interface to Opportunity.
Rename leadId properties to opportunityId.
TypeScript

// client/src/types/opportunity.ts
export interface Opportunity { // formerly Lead
  id: number;
  // ... all existing fields ...
  // leadSource?: string; // This might be better named as opportunitySource if it's about how this opportunity was generated
  // clientId?: number | null; // This remains, linking to a Client
  opportunityId?: number | null; // If this was a self-reference, ensure it's correct or remove
  // ...
}
// Update ContactIdentifier and Communication interfaces if they contain leadId
export interface ContactIdentifier {
  // ...
  // leadId?: number | null;
  opportunityId?: number | null; // Renamed
  // ...
}
export interface Communication {
  // ...
  // leadId?: number | null;
  opportunityId?: number | null; // Renamed
  // ...
}
Client-Side Components and Pages

Directory Rename: Rename client/src/components/leads/ to client/src/components/opportunities/.
File Renames:
LeadList.txt -> OpportunityList.txt
LeadForm.txt -> OpportunityForm.txt
RecentLeadsTable.txt (in dashboard) -> RecentOpportunitiesTable.txt
Page Rename: client/src/pages/leads.txt -> client/src/pages/opportunities.txt.
LeadDetailPage.txt -> OpportunityDetailPage.txt.
Code Updates:
Perform a project-wide find and replace for "Lead" -> "Opportunity", "lead" -> "opportunity", "leads" -> "opportunities" (being careful with pluralization and context). This applies to variable names, function names, state variables, UI text, comments, etc.
Update API calls to use the new /api/opportunities endpoints.
Update React Query keys: e.g., queryKey: ["/api/leads"] becomes queryKey: ["/api/opportunities"].
Routing (client/src/App.txt, client/src/components/layout/Sidebar.txt):
Update routes from /leads to /opportunities.
Update navigation links in the sidebar.
client/src/App.txt:
TypeScript

// import Leads from "@/pages/leads";
import Opportunities from "@/pages/opportunities"; // Renamed
// ...
// <Route path="/leads" component={Leads} />
// <Route path="/leads/new" component={Leads} />
// <Route path="/leads/:id" component={Leads} />
// <Route path="/leads/:id/edit" component={Leads} />
<Route path="/opportunities" component={Opportunities} />
<Route path="/opportunities/new" component={Opportunities} />
<Route path="/opportunities/:id" component={Opportunities} />
<Route path="/opportunities/:id/edit" component={Opportunities} />
client/src/components/layout/Sidebar.txt:
TypeScript

// { name: "Leads", href: "/leads", icon: Filter },
{ name: "Opportunities", href: "/opportunities", icon: Filter /* Consider a new icon for Opportunity */ },