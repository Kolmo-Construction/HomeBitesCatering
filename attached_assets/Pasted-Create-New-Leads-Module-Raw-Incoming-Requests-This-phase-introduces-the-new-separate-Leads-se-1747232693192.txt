Create New "Leads" Module (Raw Incoming Requests)
This phase introduces the new, separate "Leads" section for managing raw requests.

Schema (shared/schema.ts):

Define a new table named raw_leads (or just leads if the old one is truly gone after renaming). Let's use raw_leads for clarity during transition.
TypeScript

export const rawLeadStatusEnum = pgEnum("raw_lead_status", ['new', 'under_review', 'qualified', 'archived', 'junk']);

export const rawLeads = pgTable("raw_leads", {
  id: serial("id").primaryKey(),
  source: text("source").notNull(), // e.g., 'weddingwire', 'website_form', 'gmail_sync', 'manual_entry'
  rawData: jsonb("raw_data"), // Store the original request payload/email body etc.
  extractedName: text("extracted_name"),
  extractedEmail: text("extracted_email"),
  extractedPhone: text("extracted_phone"),
  eventSummary: text("event_summary"), // Brief summary/keywords from raw_data
  receivedAt: timestamp("received_at").defaultNow().notNull(),
  status: rawLeadStatusEnum("status").default('new').notNull(),
  // This field links a raw lead to the opportunity created from it.
  createdOpportunityId: integer("created_opportunity_id").references(() => opportunities.id, { onDelete: 'set null' }),
  notes: text("internal_notes"), // For internal notes about this raw lead
  assignedToUserId: integer("assigned_to_user_id").references(() => users.id), // Optional: if raw leads can be assigned for review
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
});

export const insertRawLeadSchema = createInsertSchema(rawLeads, {
  rawData: z.any().optional(),
  receivedAt: z.coerce.date().optional(),
}).omit({
  id: true,
  createdAt: true,
  updatedAt: true,
  createdOpportunityId: true // This is set after opportunity creation
});
export type RawLead = typeof rawLeads.$inferSelect;
export type InsertRawLead = z.infer<typeof insertRawLeadSchema>;
Add these new types and schema to your main exports in shared/schema.ts.
Backend (server/storage.ts, server/routes.ts):

Storage: Implement functions in server/storage.ts for raw_leads:
createRawLead(data: InsertRawLead): Promise<RawLead>
getRawLeadById(id: number): Promise<RawLead | undefined>
listRawLeads(filters?: { status?: string; source?: string }): Promise<RawLead[]>
updateRawLead(id: number, data: Partial<RawLead>): Promise<RawLead | undefined> (for status changes, linking createdOpportunityId, adding notes).
API Routes: Create new API endpoints in server/routes.ts under /api/raw-leads:
POST /api/raw-leads: For ingesting new raw leads from various sources (WeddingWire, Zoho, website forms, manual entry for direct messages). This endpoint will call storage.createRawLead.
GET /api/raw-leads: To list raw leads for the new "Leads" page.
GET /api/raw-leads/:id: To get details of a specific raw lead.
PUT /api/raw-leads/:id: To update a raw lead (e.g., change status, add notes).
Integration Services (e.g., emailSyncService.ts):

Modify emailSyncService.ts (and create new ones for other sources like WeddingWire, SeattleFoodTruck):
These services will now call storage.createRawLead to initially store the incoming request in the raw_leads table.
The rawData field will store the original email body or API payload.
The service should attempt to parse and populate extractedName, extractedEmail, extractedPhone, and eventSummary from the raw data.
Client-Side: New "Leads" Page & Components

Routing: Create a new top-level route like /leads (or /raw-leads) in client/src/App.txt.
Page: Create client/src/pages/rawLeadsPage.txt (or leadsPage.txt).
Components: Create a new directory client/src/components/rawLeads/ (or leads/ if the old one was fully renamed).
RawLeadList.txt:
Fetches and displays raw leads from GET /api/raw-leads using DataTable.
Columns: Source, Extracted Contact Info, Event Summary, Received At, Status, Actions.
Filters: By source, status.
Action per row: "Review & Qualify" button.
(Optional) RawLeadDetailPage.txt: A view to see the full rawData and internal notes for a specific raw lead before qualifying.
Sidebar (client/src/components/layout/Sidebar.txt): Add a new navigation item "Leads" (or "Lead Inbox") pointing to this new page.
Phase 3: Implement Manual Lead-to-Opportunity Conversion
This is the core of the new workflow.

"Review & Qualify" Action on Raw Leads Page:
When a user clicks "Review & Qualify" for a raw lead in RawLeadList.txt:
Navigate to the "New Opportunity" form (/opportunities/new).
Pass the rawLeadId as a URL parameter or query string (e.g., /opportunities/new?fromRawLeadId=${rawLead.id}).
