Rename Existing "Lead" to "Opportunity"
This phase involves a significant find-and-replace operation, along with careful updates to schemas, functions, and types.

Schema Changes (shared/schema.ts)

Table Rename: Rename the leads table to opportunities.
TypeScript

// export const leads = pgTable("leads", { ... });
export const opportunities = pgTable("opportunities", {
  // ... existing fields ...
  // Ensure clientId, assignedTo still reference the correct tables (clients, users)
});
Type Renames:
Lead type to Opportunity.
InsertLead type to InsertOpportunity.
insertLeadSchema to insertOpportunitySchema.
TypeScript

// export type Lead = typeof leads.$inferSelect;
// export type InsertLead = z.infer<typeof insertLeadSchema>;
// export const insertLeadSchema = createInsertSchema(leads, { ... });

export type Opportunity = typeof opportunities.$inferSelect;
export const insertOpportunitySchema = createInsertSchema(opportunities, {
  eventDate: z.string().nullable().transform(date => date ? new Date(date) : null),
  // any other specific zod transformations for Opportunity
}).omit({
  id: true,
  createdAt: true,
  updatedAt: true,
  clientId: true // If clientId is handled during conversion/linking
});
export type InsertOpportunity = z.infer<typeof insertOpportunitySchema>;
Foreign Key Updates:
In the clients table, rename leadId to opportunityId and update its reference:
TypeScript

// leadId: integer("lead_id").references(() => leads.id),
opportunityId: integer("opportunity_id").references(() => opportunities.id),
In the contactIdentifiers table, rename leadId to opportunityId and update its reference:
TypeScript

// leadId: integer("lead_id").references(() => leads.id, { onDelete: 'cascade' }),
opportunityId: integer("opportunity_id").references(() => opportunities.id, { onDelete: 'cascade' }),
In the communications table, rename leadId to opportunityId and update its reference:
TypeScript

// leadId: integer("lead_id").references(() => leads.id, { onDelete: 'set null' }),
opportunityId: integer("opportunity_id").references(() => opportunities.id, { onDelete: 'set null' }),
Update Zod schemas (insertClientSchema, insertContactIdentifierSchema, insertCommunicationSchema) to reflect these opportunityId changes if they explicitly include these foreign keys.