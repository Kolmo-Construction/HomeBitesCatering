his is a slightly more involved change to LeadForm.tsx if you want the /leads/:id/edit route to work directly by passing just an ID.

TypeScript

// client/src/components/leads/LeadForm.tsx
// ... (imports)

interface LeadFormProps {
  lead?: FormValues; // Existing prop for pre-filled data
  isEditing?: boolean;
  onCancel?: () => void;
  leadIdForEdit?: number; // <<< NEW PROP: For fetching data when in edit mode with only an ID
}

export default function LeadForm({ lead: initialLead, isEditing = false, onCancel, leadIdForEdit }: LeadFormProps) {
  // ... (existing hooks: navigate, toast, queryClient, state for emailCheck, phoneCheck)

  const { data: fetchedLeadData, isLoading: isLoadingFetchedLead } = useQuery<FormValues>({
    queryKey: ['/api/leads', leadIdForEdit],
    queryFn: async () => {
      if (!leadIdForEdit) return undefined; // Should not happen if enabled is true
      const res = await apiRequest('GET', `/api/leads/${leadIdForEdit}`);
      return res.json();
    },
    enabled: isEditing && !!leadIdForEdit && !initialLead, // Only fetch if editing, ID provided, and no initialLead
  });

  const leadToEdit = initialLead || fetchedLeadData;

  const form = useForm<FormValues>({
    resolver: zodResolver(formSchema),
    // Default values will now consider leadToEdit
  });
  
  useEffect(() => {
    if (leadToEdit) {
      form.reset({
        ...leadToEdit,
        eventDate: leadToEdit.eventDate ? new Date(leadToEdit.eventDate) : null,
        assignToExistingClient: !!leadToEdit.clientId,
        clientId: leadToEdit.clientId ? String(leadToEdit.clientId) : "",
      });
    } else if (!isEditing) { // Reset for new form
        form.reset({ /* initial new form values */ });
    }
  }, [leadToEdit, form, isEditing]);


  if (isLoadingFetchedLead && isEditing && !!leadIdForEdit && !initialLead) {
    return <div className="text-center p-4">Loading lead data for editing...</div>;
  }
  
  // ... (rest of LeadForm.tsx logic, using `leadToEdit` where it previously used `lead`)
  // In the mutation, use `leadToEdit?.id` if isEditing
  // const mutation = useMutation({
  //   mutationFn: async (values: FormValues) => {
  //     // ...
  //     if (isEditing && leadToEdit) { // Use leadToEdit here
  //       const res = await apiRequest("PATCH", `/api/leads/${leadToEdit.id}`, leadValues);
  // ...

  // Make sure the submit handler also checks leadToEdit?.id for updates
  // ...