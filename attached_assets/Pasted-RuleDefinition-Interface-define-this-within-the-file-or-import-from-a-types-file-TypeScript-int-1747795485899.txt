RuleDefinition Interface (define this within the file or import from a types file):

TypeScript

interface RuleDefinition {
  id: string; // Unique ID for the rule (can be temp client-side ID initially)
  // triggerQuestionId: string; // Implicitly the selectedQuestion.id for this design
  conditionType: formSchema.conditionalLogicConditionTypeEnum;
  conditionValue?: string; // Value to check against the selectedQuestion's answer
  actionType: formSchema.conditionalLogicActionTypeEnum;
  targets: Array<{
    targetType: 'question' | 'page';
    targetId: string; // ID of the formPageQuestion or formPage
  }>;
  description?: string; // Optional internal description
}
Update TabsList in QuestionSettingsPanel:

Add a new TabsTrigger for the "Logic" tab.
It should appear after "Advanced".
It should be disabled if !selectedQuestion (i.e., no question is selected in the main editor canvas). <!-- end list -->
TypeScript

<TabsTrigger value="logic" disabled={!question}>Logic</TabsTrigger>
(Based on your existing structure in QuestionLibraryEdit.tsx where questionType is used to disable tabs, you'll use !question or !selectedQuestion for the FormEditor).
New TabsContent for "Logic":

This section will display the list of rules for the selectedQuestion and an "Add Rule" button.
Layout:
A header: "Conditional Logic Rules"
"Add Rule" Button (<Button variant="outline" size="sm"> <PlusCircle /> Add New Rule </Button>).
onClick: Sets editingRule to null and isRuleEditorOpen to true.
Disabled if !selectedQuestion.
Rules List:
If conditionalRules is empty: Display a message like "No conditional rules defined for this question. Rules defined here are triggered by the answer provided to this question."
If rules exist: Map through conditionalRules and render each rule in a <Card> or similar styled container.
Rule Display: Clearly show "IF THIS QUESTION's answer... [Condition Type] [Condition Value] ...THEN [Action Type] ...TARGET(S): [Target Names]". You'll need helper functions to get target names from allQuestionsInForm and allPagesInForm using their IDs.
Actions per Rule:
Edit Button (<Settings /> icon): onClick sets editingRule to the current rule and isRuleEditorOpen to true.
Delete Button (<Trash2 /> icon): onClick prompts for confirmation and then removes the rule from conditionalRules (and eventually calls a delete mutation).
RuleEditorDialog Component (or inline JSX for the dialog):

Controlled by isRuleEditorOpen.
Receives editingRule, allQuestionsInForm (to select target questions), allPagesInForm (to select target pages), onSaveRule, onCancel.
The triggerQuestion for the rule is implicitly the selectedQuestion in the FormEditor.
Form Fields inside the Dialog (using react-hook-form and Zod schema for validation):
(Display Only): "Condition based on: [Name of selectedQuestion]"
Condition Type (conditionType):
Component: <Select>
Options: Populated from formSchema.conditionalLogicConditionTypeEnum.enumValues. Convert enum values (e.g., is_selected_option_value) to user-friendly labels (e.g., "Is Selected Option Value").
Condition Value (conditionValue):
Component: <Input type="text" />
Visibility: Show this field only if conditionType requires a value (e.g., hide for is_filled, is_not_filled).
Dynamic Input: For more advanced behavior, if selectedQuestion.questionType is 'dropdown' or 'radio_group', this conditionValue input could dynamically change to a <Select> populated with selectedQuestion.options.
Action Type (actionType):
Component: <Select>
Options: Populated from formSchema.conditionalLogicActionTypeEnum.enumValues. Convert enum values to user-friendly labels.
Target Type (targetType):
Component: <Select>
Options: "Question", "Page".
Targets:
If targetType is "Question":
Component: A multi-select component (if available in your UI library) or a list of <Checkbox> components.
Options: Populate from allQuestionsInForm, filtering out the selectedQuestion itself to prevent simple self-targeting loops for actions like show/hide.
Label for each checkbox/option: question.displayText. Value: question.id.
If targetType is "Page":
Component: Multi-select or list of Checkboxes.
Options: Populate from allPagesInForm.
Label: page.pageTitle. Value: page.id.
Description (Optional): <Textarea> for an internal note about the rule.
Dialog Footer: "Save Rule" and "Cancel" buttons.
"Save Rule": Calls onSaveRule with the rule data.
"Cancel": Closes the dialog.