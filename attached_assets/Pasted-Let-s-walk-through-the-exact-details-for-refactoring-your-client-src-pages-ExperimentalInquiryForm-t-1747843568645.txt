Let's walk through the exact details for refactoring your client/src/pages/ExperimentalInquiryForm.tsx file.

We'll proceed step-by-step:

📁 Step 1: Isolate Large Static Data
Create a data directory:
Inside client/src/, create a new folder named data.

Create client/src/data/dessertOptions.ts:

Move the following from ExperimentalInquiryForm.tsx into this new file:
type DessertLotSize = 36 | 48 | 72 | 96 | 144;
type DessertItem = { id: string; name: string; price: number; };
const dessertItems: DessertItem[] = [ ... ]; (the entire array)
const dessertLotSizes: DessertLotSize[] = [ ... ]; (the entire array)
Ensure you export all these types and constants:
TypeScript

// client/src/data/dessertOptions.ts
export type DessertLotSize = ...;
export type DessertItem = ...;
export const dessertItems: DessertItem[] = ...;
export const dessertLotSizes: DessertLotSize[] = ...;
Create client/src/data/eventOptions.ts:

Move the following from ExperimentalInquiryForm.tsx:
The lucide-react imports specifically for the event type icons: Cake, Calendar, Gift, Users, Truck, Wine, Utensils.
type EventType = | "Wedding" | ... | "Other Private Party";
const eventTypes: { type: EventType; ... }[] = [ ... ]; (the entire array)
Ensure React is imported if React.ReactNode is used, and export the type and constant:
TypeScript

// client/src/data/eventOptions.ts
import React from 'react';
import { Cake, Calendar, /* ...other icons... */ } from "lucide-react";
export type EventType = ...;
export const eventTypes: { ... }[] = ...;
Create client/src/data/menuAndAppetizerData.ts:

Move the following from ExperimentalInquiryForm.tsx:
const appetizerData = { ... }; (the entire object)
const horsDoeurvesData = { ... }; (the entire object)
const themeMenuData = { ... }; (the very large object)
const horsDoeurvesComponentData = { ... }; (the entire object)
Ensure you export all these constants:
TypeScript

// client/src/data/menuAndAppetizerData.ts
export const appetizerData = { ... };
export const horsDoeurvesData = { ... };
export const themeMenuData = { ... };
export const horsDoeurvesComponentData = { ... };
Note: If these data objects use specific types that are only relevant to them and not shared elsewhere, those types can also be moved into this file or kept alongside the data definitions.
🧩 Step 2: Separate Major Step Components
Create a formSteps directory:
Inside client/src/components/, create a new folder named formSteps.

Create a shared types file: client/src/types.ts:

Move the following type definitions from ExperimentalInquiryForm.tsx into this new file:
type FormStep = | "eventType" | ... | "review";
type EventInquiryFormData = { ... }; (the entire large type definition)
Ensure EventType and other necessary types are imported if EventInquiryFormData depends on them (e.g., import { EventType } from "../data/eventOptions";).
Export these types:
TypeScript

// client/src/types.ts
import { EventType } from "../data/eventOptions"; // Adjust path as needed

export type FormStep = ...;
export type EventInquiryFormData = {
  eventType: EventType | null; // Example of using imported type
  // ... rest of the fields
};
For each step component (EventTypeSelectionStep, BasicInformationStep, EventDetailsStep, MenuSelectionStep, AppetizersQuestionStep, AppetizersStep, DessertsStep):

Create a new .tsx file in client/src/components/formSteps/. For example, for BasicInformationStep, create client/src/components/formSteps/BasicInformationStep.tsx.
Inside the new file (e.g., BasicInformationStep.tsx):
Copy the entire functional component definition (e.g., const BasicInformationStep = ({ eventType, onPrevious, onNext }: { ... }) => { ... return (...); };) from its original location in ExperimentalInquiryForm.tsx.
Add necessary imports at the top of the new file:
import React from "react"; (and other React hooks like useEffect if used within that specific component).
import { useFormContext, Controller } from "react-hook-form"; (if used).
Imports for UI components from @/components/ui/... (e.g., Button, Input, Card, etc.).
Imports for lucide-react icons if that step uses them directly.
Import data it directly consumes (e.g., EventTypeSelectionStep will need import { eventTypes } from "../../../data/eventOptions"; - adjust path). DessertsStep will need dessertItems and dessertLotSizes. AppetizersStep will need appetizerData, horsDoeurvesData. MenuSelectionStep will need themeMenuData.
Import shared types like EventInquiryFormData from ../../../types; and EventType from ../../../data/eventOptions.
Define the props interface for the component (e.g., interface BasicInformationStepProps { ... }).
Add export default YourStepComponentName; at the end.
Example structure for client/src/components/formSteps/DessertsStep.tsx:
```typescript
// client/src/components/formSteps/DessertsStep.tsx
import React from "react";
import { useFormContext } from "react-hook-form";
import { Button } from "@/components/ui/button";
import { ChevronLeft, ChevronRight } from "lucide-react";
import { dessertItems, dessertLotSizes, DessertItem, DessertLotSize } from "../../../data/dessertOptions"; // Adjust path
import { EventInquiryFormData } from "../../../types"; // Adjust path

interface DessertsStepProps {
  onPrevious: () => void;
  onNext: () => void;
}

const DessertsStep: React.FC<DessertsStepProps> = ({ onPrevious, onNext }) => {
  const { setValue, watch } = useFormContext<EventInquiryFormData>();
  // ... rest of the DessertsStep logic and JSX copied from original file ...

  // The DessertMatrix component definition can also be moved into this file
  // if it's only used by DessertsStep, or into its own file if used elsewhere.
  // For now, assume it's co-located or imported if already separate.
  const DessertMatrix = ({ item, onSelectionChange }: { /* ...props... */ }) => {
     // ... DessertMatrix logic ...
  };


  return (
    // ... JSX for DessertsStep ...
  );
};

export default DessertsStep;
```
Co-locate sub-components: If a step component has a helper component defined within its scope (like DessertMatrix inside DessertsStep, or EventTypeCard inside EventTypeSelectionStep), you can either keep it within the new step component's file or move it to its own file in a sub-directory like client/src/components/formSteps/shared/ if it's used by multiple steps. For simplicity, co-locating it in the step's file is fine if it's only used there.
🛠️ Step 3: Externalize Utility Functions
Create a utils directory:
Inside client/src/, create a new folder named utils.

Create client/src/utils/formUtils.ts:

Move the following functions from ExperimentalInquiryForm.tsx (or from where they are defined, e.g., AppetizersStep might contain calculateCategoryTotal and calculateHorsDoeurvesTotal):
function validateEventType(type: string): boolean { ... }
function mapUrlToEventType(type: string): EventType | null { ... }
The calculateCategoryTotal function that was part of HorsDoeurvesMatrix logic (if you decide to make it a general util).
The calculateHorsDoeurvesTotal function from AppetizersStep.
Ensure these functions import any necessary types (e.g., EventType from ../data/eventOptions).
Export all functions:
TypeScript

// client/src/utils/formUtils.ts
import { EventType } from "../data/eventOptions"; // Adjust path

export function validateEventType(type: string): boolean { ... }
export function mapUrlToEventType(type: string): EventType | null { ... }
// ...
export const calculateHorsDoeurvesTotal = (horsDoeurvesSelections: any, guestCount: number): number => { ... };
🚀 Step 4: Refactor ExperimentalInquiryForm.tsx & Implement Lazy Loading
Your main client/src/pages/ExperimentalInquiryForm.tsx will now be significantly smaller and act as the orchestrator.

Clean up existing definitions:

Remove the large static data definitions (already moved in Step 1).
Remove the inline definitions of the step components (moved in Step 2).
Remove the inline definitions of utility functions (moved in Step 3).
Update Imports:

Keep essential React imports: import React, { useState, useEffect, Suspense } from "react";
Keep imports for Helmet, useForm, FormProvider, Form (from react-hook-form and @/components/ui/form).
Import shared types: import { EventInquiryFormData, FormStep } from "../types"; (adjust path).
Import EventType if needed directly: import { EventType } from "../data/eventOptions"; (adjust path).
Import utilities: import { mapUrlToEventType } from "../utils/formUtils"; (adjust path).
Crucially, import your step components using React.lazy:
TypeScript

const EventTypeSelectionStep = React.lazy(() => import('../components/formSteps/EventTypeSelectionStep'));
const BasicInformationStep = React.lazy(() => import('../components/formSteps/BasicInformationStep'));
const EventDetailsStep = React.lazy(() => import('../components/formSteps/EventDetailsStep'));
const MenuSelectionStep = React.lazy(() => import('../components/formSteps/MenuSelectionStep'));
const AppetizersQuestionStep = React.lazy(() => import('../components/formSteps/AppetizersQuestionStep'));
const AppetizersStep = React.lazy(() => import('../components/formSteps/AppetizersStep'));
const DessertsStep = React.lazy(() => import('../components/formSteps/DessertsStep'));
Define PublicFormHeader and FormProgressBar:

These can remain defined within ExperimentalInquiryForm.tsx if they are not too large or complex. Alternatively, move them to their own files (e.g., client/src/components/layout/PublicFormHeader.tsx) and import them normally (not lazily, as they are likely part of the main layout).
Add a Suspense Fallback Component:

Define a simple loading component, or import a more sophisticated one:
TypeScript

const StepLoadingSpinner = () => (
  <div className="flex justify-center items-center h-96">
    <p>Loading...</p> {/* Or your preferred spinner */}
  </div>
);
Modify the return statement:

The main structure (Helmet, FormProvider, Form, form element) remains.
Wrap the conditional rendering block for your steps with <Suspense fallback={<StepLoadingSpinner />}>:
TypeScript

// ... inside PublicInquiryForm component
return (
  <>
    <Helmet>...</Helmet>
    <div className="min-h-screen bg-gray-50 pb-12">
      <PublicFormHeader />
      <FormProvider {...methods}>
        <Form {...methods}>
          <form noValidate className="space-y-8">
            {currentStep !== "eventType" && (
              <div className="container mx-auto px-4 max-w-3xl">
                <FormProgressBar currentStep={currentStepNumber} totalSteps={totalSteps} />
              </div>
            )}
            <Suspense fallback={<StepLoadingSpinner />}>
              {currentStep === "eventType" && (
                <EventTypeSelectionStep
                  onSelectEventType={handleEventTypeSelect}
                  selectedEventType={eventType}
                />
              )}
              {currentStep === "basicInfo" && eventType && (
                <BasicInformationStep
                  eventType={eventType}
                  onPrevious={handlePrevious}
                  onNext={handleNext}
                />
              )}
              {/* ... and so on for all other step components ... */}
            </Suspense>
          </form>
        </Form>
      </FormProvider>
    </div>
  </>
);
State Management and Logic:

The useForm hook with defaultValues remains here.
The useState for currentStep remains here.
The steps array definition remains here.
The navigation handlers (handleEventTypeSelect, handlePrevious, handleNext) remain here.
Any watch calls from react-hook-form for values needed by the main orchestrator logic (like eventType, requestedTheme, guestCount for navigation) remain here.
After these changes, client/src/pages/ExperimentalInquiryForm.tsx will be significantly smaller and primarily focused on orchestrating the form flow, while the data, step-specific logic, and utilities will reside in their dedicated, more manageable files. Remember to adjust import paths (../) carefully based on your exact directory structure.