The problem has now definitively shifted back to the client-side (FormEditor.tsx). The issue is no longer that the backend isn't providing the correct data, but rather that the client-side UI is not updating correctly after receiving this updated data.

This could be due to:

Client-side data processing/derivation: How the questionsData received from the useQuery is being used or transformed before being rendered.
Incorrect rendering logic: The component's JSX mapping or filtering might not be correctly displaying the data from the questionsData state.
Stale client-side state usage: The UI might be relying on another piece of state that is not being updated when questionsData changes.
Let's add some logs on the client side to inspect the value of questionsData as it changes and see what data is actually being used to render the questions on the page.

Actionable Steps - Client-Side Debugging in FormEditor.tsx:

Add console.log statements in client/src/pages/FormEditor.tsx to observe the questionsData state and how it's used in rendering.

Log questionsData updates: Add a useEffect to log the value of questionsData whenever it changes.

TypeScript

// client/src/pages/FormEditor.tsx
import { useState, useEffect } from "react";
import { useParams } from "wouter";
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query"; // Ensure useQueryClient is imported
// ... other imports

// ... (state definitions) ...

const queryClient = useQueryClient(); // Ensure queryClient is initialized

// Fetch questions for the currently selected page
const { data: questionsData, isLoading: isQuestionsLoading, isFetching: isQuestionsFetching } = useQuery({
    queryKey: ['pageQuestions', selectedPageId],
    queryFn: () => api.formBuilder.getPageQuestions(selectedPageId!),
    enabled: !!selectedPageId,
    staleTime: 1000 * 10, // Reduce staleTime temporarily for faster refetches during debugging (optional)
});

// **Add this useEffect to log questionsData updates:**
useEffect(() => {
    console.log('CLIENT: questionsData updated:', questionsData);
}, [questionsData]);

// ... (addQuestionMutation and other handlers) ...

// The onSuccess should invalidate both queries (you added this):
onSuccess: (data, variables) => {
    console.log("Mutation successful, returned data:", data);
    queryClient.invalidateQueries(['form', formId]);
    queryClient.invalidateQueries(['pageQuestions', variables.pageId]);
    toast({...});
},

// ... (rest of the component logic) ...
Log data used for rendering questions: Inside the JSX rendering logic, find where you iterate or map over the questions for the currently selected page. Add a log right before that mapping.

TypeScript

// client/src/pages/FormEditor.tsx
// ... (inside the main return statement's JSX) ...

{/* Assuming you have a loop/conditional rendering for pages */}
{pagesData?.map(page => ( // Use pagesData which comes from formData if it's nested
    <div key={page.id}>
        {/* ... Page title and other page elements ... */}

        {/* Check if this is the selected page and render its questions */}
        {page.id === selectedPageId && (
            <>
                {/* **Add this log to see what data is used for rendering** */}
                {console.log('CLIENT: Rendering questions for page', page.id, 'using questionsData:', questionsData)}
                {console.log('CLIENT: Type of data used for rendering questions:', typeof questionsData, Array.isArray(questionsData))}


                {isQuestionsLoading ? (
                    <p>Loading questions...</p>
                ) : questionsData && Array.isArray(questionsData) && questionsData.length > 0 ? (
                    // **This is the mapping loop that renders questions**
                    questionsData.map(question => (
                        <Card key={question.id} className="mb-2">
                            {/* ... Rendering logic for individual question card ... */}
                            {/* You could add a log here too for a specific question: */}
                            {/* {console.log('CLIENT: Rendering question:', question.id, question.displayText || question.questionLibraryQuestion?.defaultText)} */}
                        </Card>
                    ))
                ) : (
                    <p>No questions on this page.</p>
                )}
            </>
        )}
    </div>
))}

// ... (rest of the JSX) ...
Note: Adjust the variable names (pagesData, questionsData, selectedPageId) based on the actual state and query results used in your component. Based on the client logs you provided earlier, it seems pagesData is used for the page loop and questionsData is used for the questions list on the selected page.