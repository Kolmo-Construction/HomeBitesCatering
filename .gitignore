node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Secrets — never commit
.env
.env.*
.Deepseek

# Recipe migration data — contains recipe IP, never commit
scripts/recipesgen-cards.json
scripts/recipesgen-*.jsonbackup_*.dump
