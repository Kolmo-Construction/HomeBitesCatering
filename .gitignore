node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Secrets — never commit
.env
.env.*
.Deepseek
.infra_keys

# Recipe migration data — contains recipe IP, never commit
scripts/recipesgen-cards.json
scripts/recipesgen-*.json

# Local database dumps / backups — never commit
backup_*.dump
database-backup-*.sql
database-backup-*.sql.gz
database-dump-*.sql
*.dump
my-database-backup.sql
database_backup.sql
